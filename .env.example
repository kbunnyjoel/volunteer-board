# Server
PORT=4000

# Supabase
# - SUPABASE_URL: Project API URL (Settings → API)
# - SUPABASE_SERVICE_ROLE_KEY: Service key used by the backend (keep private)
# - SUPABASE_ANON_KEY: Optional anon key for frontend usage
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=supabase-service-role-key
SUPABASE_ANON_KEY=supabase-anon-key
ADMIN_SECRET=local-admin-token
# Comma-separated list of admin emails allowed to access /admin when using Supabase Auth
ADMIN_EMAILS=admin@example.com
# Comma separated; supports wildcard domains (e.g. *.vercel.app)
CLIENT_ORIGIN=http://localhost:5173
# Optional logging configuration (Logflare or compatible ingestion endpoint)
LOG_LEVEL=info
LOGFLARE_SOURCE_ID=
LOGFLARE_API_KEY=
LOGFLARE_ENDPOINT=https://api.logflare.app/logs
# Shared secret used by the frontend when posting client-side logs to the API
LOG_INGEST_TOKEN=
